node_modules
dist
agent
*.log
.DS_Store

# Use bun — keep only bun.lock
package-lock.json
pnpm-lock.yaml

# local dev worktrees
.worktrees/

# screenshot-test render artifacts (regenerated by every test run)
test/__screenshots__/
